#set(${PROJECT_NAME}_COMPONENTS)
#set(${PROJECT_NAME}_LIBRARIES)
#foreach(component ${PROJECT_NAME}_FIND_COMPONENTS)

##set(cyber_COMPONENTS)
##set(cyber_LIBRARIES)
##set(cyber_INCLUDE_DIR @CMAKE_INSTALL_INCLUDE@)
##foreach(component ${@PROJECT_NAME@_FIND_COMPONENTS})
##    include(@CMAKE_INSTALL_SHAREDIR@/${component}/cmake/@ROOT@-${component}-config.cmake)
##    list(APPEND cyber_COMPONENTS ${component})
##    #list(APPEND cyber_LIBRARIES ${component}_LIBRARIES)
##    list(APPEND cyber_LIBRARIES @PROJECT_NAME@::${component})
##endforeach()
#add_library(cyber::proto ALIAS @PROJECT_NAME@)
set(cyber_proto_LIBRARIES @PROTO_LIBS@)
set(cyber::proto @PROTO_LIBS@)
